---
title: "Quickstart"
description: "Zero to your first agent trace in 5 minutes."
---

## What you'll build

By the end of this page you'll have LangSight running locally and capturing
every tool call your AI agent makes — with full traces visible in both the
CLI and the dashboard.

## Prerequisites

- Python 3.11+
- Docker Desktop (recommended) — or skip Docker for CLI-only use

---

## 1. Choose your path

<Tabs>
  <Tab title="Full dashboard (recommended)">
    Get the full stack running in one command:

    ```bash
    git clone https://github.com/LangSight/langsight && cd langsight
    ./scripts/quickstart.sh
    ```

    The script builds the Docker images (3–5 min on first run) then starts everything. When it's done you'll see:

    ```
    ──────────────────────────────────────────────────────
      LangSight is running!
    ──────────────────────────────────────────────────────

      Dashboard:  http://localhost:3003
      API:        http://localhost:8000

      Login credentials:
        Email:    admin@langsight.dev
        Password: ls-ceab2f29f600               ← your generated password

      API Key:    ls_2dc63dbddeb...

      Services:
        postgres     healthy
        clickhouse   healthy
        api          healthy
        dashboard    healthy
    ──────────────────────────────────────────────────────
    ```

    All 4 services must show **healthy**. Open **http://localhost:3003** and log in with the credentials printed above.

    <Note>
      Credentials are also saved in `.env`. If you lose them:
      ```bash
      grep LANGSIGHT_ADMIN .env    # email + password
      grep LANGSIGHT_API_KEYS .env # API key
      ```
    </Note>

    If a service fails, the script prints its logs automatically and tells you exactly what to do.
  </Tab>
  <Tab title="CLI only (no Docker)">
    The CLI works immediately with zero config — no server, no database:

    ```bash
    pip install langsight           # or: uv tool install langsight
    langsight init                  # discovers MCP servers from Claude/Cursor/VS Code
    langsight mcp-health            # instant health check results
    langsight security-scan         # OWASP + CVE scan findings
    ```

    Results are displayed in the terminal. When you're ready for the full
    dashboard experience, come back and run `./scripts/quickstart.sh`.
  </Tab>
</Tabs>

### Quickstart flags

| Flag | What it does |
|------|-------------|
| *(none)* | Start / resume the stack |
| `--redis` | Also start Redis (enables multi-worker mode and SSE broadcasting across workers) |
| `--reset` | Wipe all volumes + `.env` and start completely fresh |
| `--build` | Force-rebuild Docker images (pick up code changes) |

Flags can be combined:

```bash
./scripts/quickstart.sh --reset          # clean slate
./scripts/quickstart.sh --redis          # start with Redis
./scripts/quickstart.sh --reset --build  # clean slate + fresh images
```

### Troubleshooting the quickstart

<AccordionGroup>
  <Accordion title="API is unhealthy — password authentication failed">
    Your Postgres volume was initialised with a different password than your current `.env`.
    This happens when `.env` was deleted or regenerated while volumes still existed.

    ```bash
    ./scripts/quickstart.sh --reset
    ```

    `--reset` wipes both the volumes and `.env` so they're always in sync.
  </Accordion>
  <Accordion title="Script times out after ~4 minutes">
    On a slow machine or first run the cold-start chain (postgres → clickhouse → api → dashboard)
    can take up to 3–4 minutes. If it times out, the script shows which service is still starting
    and its last 20 log lines.

    Check logs directly:
    ```bash
    docker compose logs api --tail=50
    docker compose logs clickhouse --tail=50
    ```
  </Accordion>
  <Accordion title="Port already in use">
    If ports 3003, 8000, 5432, or 8123 are taken by another service, the script warns you before starting.

    Find what's using a port:
    ```bash
    lsof -i :8000   # macOS / Linux
    netstat -ano | findstr :8000   # Windows
    ```
  </Accordion>
  <Accordion title="Running on Windows">
    Use **Git Bash** (included with Git for Windows) or **WSL** to run the script.
    PowerShell and CMD are not supported. Docker Desktop must be running.
  </Accordion>
</AccordionGroup>

---

## 2. Instrument your agent

Add 2 lines to your existing agent code. LangSight traces every LLM call,
every MCP tool call, and every agent handoff — automatically.

```python
import langsight
langsight.auto_patch()  # LLM + MCP + handoffs — all automatic

question = "How many orders were placed today?"

# Wrap your agent logic in a session context
async with langsight.session(
    agent_name="my-agent",
    input=question,          # records the initial human prompt
) as sess:
    # MCP calls: auto-traced — no wrap() needed
    result = await mcp_session.call_tool("query", {"sql": "SELECT 1"})
    #                                                 ↑ traced automatically
    sess.set_output(result)  # records the final agent response
```

Set environment variables before running (all three values are in your `.env`):

```bash
export LANGSIGHT_URL=http://localhost:8000
export LANGSIGHT_API_KEY=ls_your_key         # grep LANGSIGHT_API_KEYS .env
export LANGSIGHT_PROJECT_ID=your-project-id  # copy from dashboard Settings > Project ID
```

<Note>
  `auto_patch()` patches `mcp.ClientSession.call_tool`, all OpenAI/Anthropic/
  Gemini SDK classes, and enables handoff auto-detection — all at once.
  If LangSight is unreachable, your agent works normally — nothing breaks.
</Note>

If you need explicit control over which MCP sessions are traced (e.g. different
`redact_payloads` per server), use `wrap()` directly:

```python
from langsight.sdk import LangSightClient

ls = LangSightClient(url="http://localhost:8000")
traced = ls.wrap(mcp_session, server_name="postgres-mcp",
                 agent_name="my-agent", session_id="sess-001")
result = await traced.call_tool("query", {"sql": "SELECT 1"})
```

### Using a framework? Even simpler.

<Tabs>
  <Tab title="LangChain / LangGraph">
    ```python
    import langsight
    langsight.auto_patch()  # zero config — patches LangGraph automatically

    # No callbacks needed — auto_patch() injects tracing into
    # graph.stream(), graph.invoke(), and all LLM calls.
    graph = builder.compile()
    result = graph.invoke({"task": "Write a report on EU inflation"})
    ```

    See [LangGraph Integration](/sdk/integrations/langgraph) for topology capture,
    loop detection, and budget enforcement.
  </Tab>
  <Tab title="CrewAI">
    ```python
    from langsight.integrations.crewai import LangSightCrewAICallback

    langsight = LangSightClient(url="http://localhost:8000")
    callback = LangSightCrewAICallback(client=langsight)

    crew = Crew(agents=[...], tasks=[...], callbacks=[callback])
    crew.kickoff()
    ```
  </Tab>
  <Tab title="Pydantic AI">
    ```python
    from langsight.integrations.pydantic_ai import langsight_tool

    langsight = LangSightClient(url="http://localhost:8000")

    @langsight_tool(client=langsight, server_name="postgres-mcp")
    async def query_db(sql: str) -> list[dict]:
        return await session.call_tool("query", {"sql": sql})
    ```
  </Tab>
</Tabs>

---

## 3. See your first trace

Run your agent, then check results:

<Tabs>
  <Tab title="CLI">
    ```bash
    langsight sessions
    ```

    ```
    Agent Sessions                             last 24 hours
    ──────────────────────────────────────────────────────────────
    Session       Agent         Duration   Tools   Errors   Cost
    sess-001      my-agent      1,482ms       5        0   $0.02
    ```

    Drill into a session:

    ```bash
    langsight sessions --id sess-001
    ```

    ```
    Session: sess-001  |  1 agent  |  5 tool calls  |  0 failures

    ├── [MCP] postgres-mcp/query          42ms  ✓
    ├── [MCP] postgres-mcp/list_tables    18ms  ✓
    ├── [MCP] postgres-mcp/query          89ms  ✓
    ├── [MCP] slack-mcp/send_message     210ms  ✓
    └── [MCP] slack-mcp/send_message     180ms  ✓
    ```
  </Tab>
  <Tab title="Dashboard">
    Open **http://localhost:3003** and navigate to **Sessions**.

    You'll see a full call tree with timing, status, payloads, and cost
    attribution for every tool call your agent made.
  </Tab>
</Tabs>

---

## 3 (optional). Add prevention guardrails

Prevent runaway loops and budget overruns with a few extra parameters:

```python
client = LangSightClient(
    url="http://localhost:8000",
    loop_detection=True,   # stop if agent loops the same call 3x
    max_steps=25,          # hard stop at 25 tool calls per session
    circuit_breaker=True,  # auto-disable servers after 5 consecutive failures
)
```

See [Prevention Guardrails](/sdk/python#prevention-guardrails) for full configuration options.

---

## 4. What's next

You now have full traces of every tool call. Here's what LangSight does beyond tracing:

<CardGroup cols={2}>
  <Card title="MCP Health Monitoring" icon="heart-pulse" href="/cli/mcp-health">
    Continuous uptime and latency checks for every MCP server.
    Detects schema drift and fires alerts on state changes.
  </Card>
  <Card title="Security Scanning" icon="shield-halved" href="/cli/security-scan">
    OWASP MCP Top 10 checks, CVE scanning, tool poisoning
    detection, and auth auditing — built in.
  </Card>
  <Card title="Cost Attribution" icon="coins" href="/costs-model-pricing">
    Per-agent, per-tool, per-session cost breakdown with
    model pricing database (Claude, GPT, Gemini, Llama).
  </Card>
  <Card title="SLOs & Alerting" icon="bell" href="/slos">
    Define success rate and latency SLOs per agent.
    Get Slack alerts on breaches and anomalies.
  </Card>
</CardGroup>

### Multi-agent tracing

If your agent hands off to sub-agents, LangSight captures the full tree — automatically when tools follow the `call_<agent>` naming convention:

```python
import langsight
langsight.auto_patch()

async def orchestrator(question: str):
    async with langsight.session(agent_name="orchestrator", input=question) as sess:
        await jira_mcp.call_tool("get_issue", {"id": "TASK-42"})
        # LLM selects "call_research_agent" → handoff span auto-emitted
        response = await llm.generate(question, tools=[call_research_agent_tool])
        sess.set_output(response)

async def research_agent(question: str):
    async with langsight.session(agent_name="research_agent", input=question) as sess:
        result = await confluence_mcp.call_tool("search", {"query": question})
        sess.set_output(result)
        return result
```

For explicit handoff control (no tool-name convention required):

```python
from langsight.sdk import LangSightClient

ls = LangSightClient(url="http://localhost:8000")

async with langsight.session(agent_name="orchestrator") as session_id:
    handoff = ls.create_handoff("orchestrator", "research-agent", session_id=session_id)
    researcher = ls.wrap_child_agent(mcp, "confluence-mcp", "research-agent", handoff)
    await researcher.call_tool("search", {"query": "returns policy"})
```

The dashboard renders both patterns as a single connected tree across all agents.

<CardGroup cols={2}>
  <Card title="Python SDK Reference" icon="code" href="/sdk/python">
    Full API reference with all options.
  </Card>
  <Card title="Self-Hosting Guide" icon="server" href="/self-hosting/docker-compose">
    Production deployment with Postgres + ClickHouse.
  </Card>
</CardGroup>
